---
title: "Zufallszahlen Beispiel"
format: 
  html:
    code-fold: true
---

# Zufallszahlen

In diesem Beispiel werden drei Zufallszahlen erzeugt. Klicke auf **Run** oder starte den Code erneut, um zu sehen, wie sich die Zufallszahlen jedes Mal ändern.

```{python}
import random

# Generiere drei Zufallszahlen
random_numbers = [random.randint(1, 3) for _ in range(3)]
random_numbers
```